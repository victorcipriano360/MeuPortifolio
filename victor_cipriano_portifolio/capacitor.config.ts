import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'victor_cipriano_portifolio',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
